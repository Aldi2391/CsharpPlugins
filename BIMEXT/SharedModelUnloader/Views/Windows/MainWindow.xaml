<Window x:Class="SharedModelUnloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SharedModelUnloader.ViewModels"
        xmlns:local="clr-namespace:SharedModelUnloader"
        mc:Ignorable="d"
        Title="Стартовый экран" Height="280" Width="300">
    
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <StackPanel Orientation="Vertical" Margin="10">
        <Button Content="Переход на рабочие модели"
                Height="40"
                />
        <Button Content="Выдача изменений"
                Height="40"
                Margin="0 10 0 0"
                />
        <Button Content="Принятие изменений"
                Height="40"
                Margin="0 10 0 0"
                />
        <Button Content="Историй обмена"
                Height="40"
                Margin="0 10 0 0"
                />
        <Button Content="Настройка для BIM"
                Height="40"
                Margin="0 10 0 0"
                />
    </StackPanel>
</Window>
